# Must exactly match ORIGIN_KEY in ${DATA_DIR}/env/rr-api.env. Caddy uses it only
# on the private Docker hop so rr-api can authenticate the forwarded browser URL.
ORIGIN_KEY=replace-with-the-same-random-32-plus-character-secret
